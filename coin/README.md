# Coin Project

코인 자동매매 시스템 구축을 위한 독립 작업 공간입니다.

## 경로

- 문서: `coin/docs`
- 웹 앱: `coin/web`
- 운영용 프록시: `coin/proxy`

## 실행

```bash
cd coin/proxy
npm install
npm run dev
```

다른 터미널에서:

```bash
cd coin/web
npm install
npm run dev
```
